# 2026-08-18

## MMY-FrameQuickView 项目启动（设计阶段）

- 完成 AGENTS.md 优化重写：补全技术栈（推荐 PySide6+Pillow+numpy，managed Python 3.13.12 venv，因系统 Python 3.14 已损坏）、输入结构规范（{id}_{part}/{方向}/{动作}/帧）、规则模板 JSON 设计、PyInstaller 打包命令、Git 沙箱注意事项。
- 产出 `开发文档/01-需求方案与UI设计-v0.1.md` 与 `开发文档/MMY-FrameQuickView-设计报告-v0.1.html`（含 CSS 绘制的深色 UI mockup）。
- 核心设计决策：
  - 双显示区：A 序列帧网格（100%原尺寸+单帧bbox）/ B GIF 动画（全序列并集bbox防抖动），点击网格帧联动动画定位。
  - 方向/动作用按钮全展开，缺失组合暗红标记（查漏补缺=看按钮颜色）。
  - 不做部件组合显示（时装拆分系统）。
  - 规则模板：templates/*.json，folder_pattern/parts/directions/actions/hierarchy(可反转)/frame_pattern，顶栏切换+表单化编辑。
- 待用户确认：① 技术栈是否定 PySide6；② 实际层级顺序（方向/动作 or 动作/方向）；③ 帧命名格式；④ 部件列表下拉 or 按钮；⑤ B 区是否可选棋盘格底；⑥ shadow/fills 特殊规则；⑦ A/B 左右 or 上下布局。
- 里程碑：M0 评审 → M1 骨架PR → M2 网格+bbox → M3 动画 → M4 模板编辑器+打包。

## v0.2 评审修订（同日 20:10）

- 用户确认：技术栈 PySide6 ✅；层级默认 方向/动作/帧 ✅；帧命名 0001.png ✅。
- 实测截图两个关键发现：
  1. **帧号跨动作连续编号**（E/idle=0001-0008, E/attack=0017-0024）→ 断档检测改为「动作内 min→max 连续」，不要求从 0001 开始；可选增强：同方向各动作首尾拼接查整体缺段。
  2. **父目录混排纯 ID 文件夹（505026）与 {id}_{part}（50104101_shadow）** → folder_pattern 改 `{id}(_{part})?`，部位后缀可选。
- 部件选择从下拉改为**左栏可搜索列表**（过滤框+↑↓+回车，缺漏红点），用户认为下拉/铺按钮都影响效率。
- 三份文档已同步更新至 v0.2（AGENTS.md、01-需求方案与UI设计-v0.2.md、MMY-FrameQuickView-设计报告-v0.2.html），v0.1 文件已删除。
- ⚠️ 注意：v0.1 文件疑似因沙箱 overlay 未持久化（本回合 rm 时提示不存在）——本项目文件写入后建议 ls 验证。
- 剩余待确认：B区棋盘格底？shadow/fills 部件级覆盖？左栏+左右分区布局确认？

## v0.3 定稿 + M1 骨架开发（同日 20:25 起）

- 全部 7 项待确认闭环：B 区背景=透明默认+棋盘格 toggle；**同 ID 部件可叠层显示**（shadow 固定最底层，配套校验用途；不做跨 ID 自由组合）；布局定稿。
- 模板新增 `layer_order` 字段（数组前=越底层，shadow 必须为首）。
- 文档升级 v0.3（定稿）：AGENTS.md、01-需求方案与UI设计-v0.3.md、设计报告-v0.3.html；v0.2 文件删除。
- ⚠️ 沙箱 overlay 问题复现：上一回合写的 v0.2 文件未持久化到真实磁盘（本回合 rm 提示不存在）——每回合文件写入后必须 ls 验证，且尽量在文件创建的同回合完成 git commit 落盘。
- git init（main），文档与 .gitignore 已提交（root-commit cb75840 + c3983e6）；M1 功能分支 `feature-m1-skeleton`（无斜杠命名，规避 overlay 对 refs 子目录的抹除）。
- M1 代码完成：templates/default.json、core/template.py（folder_pattern 解析，部位按长度降序防 ride_front 截断）、core/scanner.py（断档=min→max 区间连续；同 ID 分组+配套校验并集对比）、core/imageops.py（alpha/并集 bbox）、ui 四组件（drop_zone/part_list 按ID分组+红点/button_matrix 缺失暗红/grid+anim 占位）、app.py 主窗口、开发文档/M1-骨架模块.md。
- 环境：项目 .venv 用 managed Python 3.13.12 创建，pip 安装 PySide6 耗时较长（>13min）。
- 待办：冒烟测试（scanner 扫真实目录 + offscreen 实例化主窗口）→ feature 分支提交 → 询问用户是否创建 GitHub 远端仓库建 PR。
