# M1 骨架模块 开发文档

> 分支：`feature-m1-skeleton` · 依据：01-需求方案与UI设计-v0.3.md

## 范围

拖拽 + 模板加载 + 左栏分组部件列表 + 方向/动作按钮矩阵 + 缺漏标记 + 状态栏汇总。
A/B 区为占位（M2 网格渲染、M3 动画播放接入）。

## 模块清单

| 文件 | 职责 |
|------|------|
| `templates/default.json` | 默认模板（天命装规则，含 layer_order + action_rules 分方向动作基准） |
| `src/core/template.py` | Template dataclass；folder_pattern 解析（`{id}(_{part})?`，部位按长度降序匹配防 ride_front 被截断）；`action_rules`(角色类型→方向→应含动作) + `default_character_type`；`resolve_char_type`/`expected_actions` |
| `src/core/scanner.py` | 扫描入口 scan_root（自动判断单部件/父目录，可选 `char_type_of` 回调）；帧号断档检测；**分角色类型×方向**计算 `missing_actions`；同 ID 分组 + 组级缺漏 + 配套校验 |
| `src/core/namemap.py` | ID-中文名映射：自动发现 `*匹配表*.txt`、自动登记缺失 ID、内联改名回写、编码自适应；**纯 ID 行第3列=角色类型**（主角/怪物/…），部件行第3列=部位中文名 |
| `src/core/imageops.py` | alpha bbox / 并集 bbox / 裁剪加载（M2 接入 UI） |
| `src/ui/drop_zone.py` | 文件夹拖拽接收 |
| `src/ui/part_list.py` | QTreeWidget 按 ID 分组；过滤框；🔴 缺漏标记；部位金色高亮；**`setFixedWidth(220)` 锁死侧栏宽度** + `setStretchLastSection(False)` 锁列宽 + UniformRowHeight；AsNeeded 滚动条 |
| `src/ui/button_matrix.py` | 方向/动作纵向堆叠（`ButtonStack` 在 B 区左侧纵列）；`missing` 属性（来自 `missing_actions[方向]`，**修复了原 `tpl.actions` 兜底误标全员缺失的 bug**）→ 暗红样式；选中金色；`unexpected` 属性 = 该类型本方向不需要的动作（灰 disabled 感） |
| `src/ui/grid_view.py` / `anim_view.py` | M2/M3 主体；scrollbar=AsNeeded + 细透明滚动条（8px，内容不溢出时隐藏）；**M7 改造** = B 区改 QHBoxLayout，左侧 `LEFT_PANEL_WIDTH=96` 固定宽放 ButtonMatrix，右侧（title + canvas + 控制栏）stretch=1 |
| `src/app.py` | MainWindow 装配 + 全局深色样式 + 两级 splitter `setSizes()` 固定初始比例 + `setChildrenCollapsible(False)` 防折叠；状态栏按方向显示缺动作 |

## 关键决策

1. **帧号检测规则**：实测 idle=0001–0008、attack=0017–0024 跨动作连续 → `ActionData.gaps` 只查区间内断档。
2. **层级转置**：scanner 内部统一为 direction→action 矩阵；模板 hierarchy 反转时扫描后转置。
3. **配套校验**：同 ID 组内取 方向×动作 并集为参照，任一部件缺则报「配套异常」（shadow 对不上主体会被标出）。
4. **入口兼容**：`src/main.py` 同时支持 `python -m src.main` 与 PyInstaller 直接打包。
5. **中文名映射**：沿用用户手写 txt（TAB 分隔），免维护四件套——自动发现（**从拖入目录向上递归到盘根**找 `*匹配表*.txt`，任意子层级拖入都能命中）/自动登记/内联改名回写/热更新（外部编辑自动刷新左栏，watcher 重挂防原子保存丢失监听）；**M7 增**：`QSettings("MMY","FrameQuickView")` 持久化 `last_map_file` 到 HKCU\\Software\\MMY\\FrameQuickView，下次启动/任意目录拖入兜底加载（解决了「拖 `E:\Temp\新ID` 这种无父级匹配表的目录也认中文」的诉求）；顶栏「文件」菜单含「重新加载匹配表」「选择匹配表文件…」做手动兜底。实测扫描真实目录自动登记 13 个新 ID。
6. **分方向动作约定（🔴 基准）**：匹配表纯 ID 行第3列标注角色类型（`template.CHAR_TYPE_ALIASES` 归一化：主角/hero→protagonist；伙伴/怪物/boss→non_protagonist；npc→npc；坐骑→mount；翅膀→wings；未标注→`default_character_type=non_protagonist`）。`action_rules`(类型→方向→应含动作) 基准：
   - **protagonist**：E/N/S → idle,attack,run；NW/SE → idle,run,attack,skill,hurt,block,dead,ride_idle,ride_run
   - **non_protagonist（伙伴/怪物/boss）**：E/N/S → idle,run；NW/SE → idle,run,attack,skill,hurt,block,dead
   - **npc**：全方向仅 idle,run
   - **wings（翅膀）**：全方向仅 idle,run（无 attack/skill/hurt/block/dead）
   - **mount（坐骑）**：全方向仅 ride_idle,ride_run（无 idle/run/attack/skill/hurt/block/dead）
   - 缺动作按「角色类型×方向」基准逐方向比对；按钮矩阵三态：缺=红 / 该类型不需=灰(disabled 感) / 正常=普通。红点=缺方向/缺动作/帧断档/配套异常任一。
7. **整体资源结构检测兜底（M15）**：`parse_folder_name` 只认 `{id}_{part}` 和纯 ID（字母数字），新工程命名 `{id}_{中文名}`（如 `503026_神龙`）会被漏掉。scanner 层加 `_looks_like_part_folder`：子目录含 ≥2 个已知 direction/action 即视为整体资源（part=None，res_id 取文件夹名数字前缀）。分类文件夹（如 `504004_黑龙王`，子目录是 `Render_Output`）不命中，继续下钻。`scan_part` 和 `_find_part_folders` 统一走 `_parse_folder_or_struct`。
8. **左栏右键复制（M16）**：左栏 ID 列表支持右键菜单复制到剪贴板——组头/子项均提供「复制 ID」「复制 ID · 中文名」（有中文名时）；子项额外提供「复制文件夹路径」。菜单样式沿用深色主题（选中态金色）。

## 验证

- [x] scanner 对真实目录 `E:\XYJProject\美术资源\动画序列帧\角色输出图` 扫描（164 部件 / 157 ID 组 / 0 忽略）
- [x] 匹配表加载 + 自动登记 + 回写（临时副本测试，不污染真实文件）
- [x] offscreen 模式实例化 MainWindow（QT_QPA_PLATFORM=offscreen），列表中文名/状态栏/按钮矩阵均正常
- [ ] 用户本机 GUI 实测（拖入父目录 → 列表/按钮/缺漏标记）

## 环境坑记录（重要）

- pip 官方源装 PySide6 27 分钟未完成 → **清华镜像 3 秒下完**（`-i https://pypi.tuna.tsinghua.edu.cn/simple`）
- 被杀的安装进程留下残留 → 后续安装时 pip 覆盖写触发 WorkBuddy 安全删除拦截（safe-delete fail-closed / 批量删除守卫），装出"只有 .pyi 没有 .pyd"的残包
- 解法：**手动 rm -rf 清掉 PySide6/shiboken6 残留后干净重装**（干净写入不触发删除拦截）
