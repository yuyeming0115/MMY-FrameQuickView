# MMY-FrameQuickView

## 项目概述

**MMY-FrameQuickView** 是一个用于预览全序列帧的极速桌面工具，主要功能：
- **查漏补缺**：检测文件夹结构是否符合模板规范（缺方向/缺动作/帧号断档）
- **GIF动画效果检验**：动态播放帧序列，可调速、逐帧步进
- **同ID部件叠层**：同 ID 的多部件（如 `50103101_shadow` + `50103101_weapon`）可叠层显示，
  shadow 固定最底层，用于检查资源是否配套正确（**不做**跨 ID 自由搭配组合）

## 技术栈

> ✅ 技术栈已于 2026-08-18 评审确认（v0.2）。

| 层 | 选型 | 说明 |
|----|------|------|
| 语言 | Python 3.13 | 使用 WorkBuddy managed Python（系统 Python 已损坏，禁用） |
| GUI | PySide6 | 拖拽、按钮矩阵、双显示区、左栏部件列表 |
| 图像 | Pillow + numpy | alpha 包围盒裁剪、帧分析 |
| 配置 | JSON | 规则模板存于 `templates/*.json` |
| 打包 | PyInstaller `--onefile --windowed` | 单文件 exe |

> ⚠️ 环境注意：用户系统 Python 3.14 已损坏（encodings 导入失败），开发一律使用
> `C:\Users\EDY\.workbuddy\binaries\python\versions\3.13.12\python.exe` 建 venv。

## 文件夹结构规范

### 输入结构（模板驱动，层级可配置）

默认层级为「方向 → 动作」（已确认）：

```
{拖拽根目录}/
└── {资源ID}[_{部位}]/          # 两种都合法：501421004_body 或纯ID 505026
    └── {方向}/                 # E, N, NW, S, SE
        └── {动作}/             # idle, run, attack ...
            ├── 0001.png        # 帧序列，4位补零
            ├── 0002.png
            └── ...
```

> ⚠️ 帧号规则（实测确认）：**各动作帧号不从 0001 重启，而是同方向内跨动作连续编号**。
> 例：E/idle = 0001–0008，E/attack = 0017–0024。
> 缺帧检测 = 检查该动作 min→max 区间内是否连续，**不要求从 0001 开始**。

父级目录实测示例（E:\XYJProject\美术资源\动画序列帧\角色输出图）：
```
505026/                  ← 纯ID文件夹（无部位后缀，直接含 E/N/NW/S/SE）
505027/
5030421/
50104101_shadow/         ← {id}_{part} 文件夹
50104101_wings/
50302301_ride_back/
50302301_ride_front/
```

示例根目录（每个文件夹 = 一个部件）：
```
E:\Temp\天命装\50103101_shadow
E:\Temp\天命装\50103101_weapon
E:\Temp\天命装\50151101_hair
E:\Temp\天命装\50152101_body
...
```

> ⚠️ 特效扁平结构（M21，模板 categories 规则识别，默认 50105 前缀）：
> 无方向/动作层级，图片直接在根目录，命名 `前缀_序号.png`（非四位补零）。
> 例：`50105101/changrao_1.png ~ changrao_20.png` → 虚拟方向「特效」+ 动作 changrao。
> 只做帧号断档检测，不做方向/动作查漏。

> ⚠️ 套装父级结构（M23）：一套职业装 = 多个**不同资源 ID** 的部件放同一父文件夹：
> ```
> hero\角色\Render_Output\501521005_女主2_女侠客(双刀)_部件\
>     501031005_shadow / 501031005_weapon / 501511005_hair / 501521005_body ...
> 新ID\月河郡主（琴）\
>     501031004_shadow / 501420004_hair / 501421004_body ...
> ```
> 套装判定（纯结构，不看命名）：同父直接子部件 ≥2 个、跨 ≥2 ID、总数 ≤ `outfit_merge_max`（默认16）、
> 全部同一资源类别 → 合并为一个「套装组」整装叠层预览；否则安全降级为按 ID 分组。
> 组头 = 文件夹名去 `_部件` 后缀 + 「套装」标记，不可 F2 改名；res_id/角色类型取 body 部件。

### 默认模板（templates/default.json）

| 类别 | 名称 |
|------|------|
| 部位 | hair, body, weapon, wings, ride_front, ride_back, shadow, fills |
| 方向 | E, N, NW, S, SE |
| 动作 | idle, run, attack, skill, hurt, block, dead, ride_idle, ride_run |
| 层级顺序 | direction → action（已确认为默认；模板可反转为 action → direction） |
| 帧命名 | `0001.png` 四位补零；同方向内跨动作连续编号（idle 0001-0008, attack 0017-0024） |
| 叠层顺序 | layer_order（模板可配）：shadow 最底 → body → hair → weapon → wings → fills… |

### 规则模板系统

- 模板为 JSON 文件，存于 `templates/` 目录，程序启动时自动扫描
- 字段：`name`、`folder_pattern`（`{id}(_{part})?`，部位后缀可选）、`parts`、`directions`、`actions`、`hierarchy`、`layer_order`、`frame_pattern`、`extensions`、`outfit_merge_max`（套装合并阈值，默认 16）
- 顶栏可切换模板 + 进入编辑；针对不同项目各存一份模板

### 部件选择（拖入父级文件夹时）

- **不用下拉、不铺按钮**：左侧栏为**可搜索部件列表**（顶部过滤框 + 上下键导航 + 回车打开）
- 列表按 ID 分组：组头 = `资源ID · 中文名`（匹配表映射），组内 = 各部位项；有缺漏的项带红点标记
- 套装组（M23）：组头 = 父文件夹名（去 `_部件` 后缀）+ 「套装·N件」标记，组内子项带所属 ID；重名部件（如双 shadow）toggle chips 显示为 `部位中文名·ID`
- 纯 ID 与 `{id}_{part}` 混排，部位后缀高亮显示

### 中文名映射（v0.3 确认，M1 已实现）

- 数据源：`*匹配表*.txt`（TAB 分隔、`#` 注释），拖入目录及父级**自动发现**
- **自动登记**：新 ID 自动追加 `ID⇥（待命名）`，维护=补空
- **内联改名**：左栏组头 F2/单击选中态改名，回车写回 txt；**热更新**监听外部编辑

### 同ID部件叠层（v0.3 确认）

- 选中某部件时，若同 ID 存在其他部件，方向/动作按钮区下方出现**叠层 chips**（可勾选参与层）
- 叠放顺序按模板 `layer_order`，**shadow 固定最底层**；各层共用同一并集 bbox 窗口对齐
- A 区网格与 B 区动画均按叠层合成显示；B 区背景默认透明融入 UI，**可切换棋盘格底**（查 alpha 毛边）

## 项目结构

```
MMY-FrameQuickView/
├── AGENTS.md
├── src/
│   ├── main.py                # 入口
│   ├── app.py                 # 主窗口
│   ├── core/
│   │   ├── scanner.py         # 文件夹扫描 + 模板匹配 + 缺漏检测 + 同ID分组/套装组检测
│   │   ├── namemap.py         # ID-中文名映射（自动发现/登记/内联改名回写/热更新）
│   │   ├── template.py        # 规则模板加载/保存/编辑
│   │   ├── layer.py           # 同ID叠层合成（layer_order 排序、共享bbox对齐）
│   │   └── imageops.py        # 包围盒裁剪（并集bbox）、帧加载
│   └── ui/
│       ├── drop_zone.py       # 拖拽区
│       ├── part_list.py       # 左栏部件列表（可搜索、分类chips过滤、缺漏红点）
│       ├── button_matrix.py   # 方向/动作按钮组（含缺失状态标记、扁平特效虚拟方向）
│       ├── flow_layout.py     # 流式换行布局（分类 chips 行）
│       ├── grid_view.py       # 序列帧网格显示区（A区）
│       └── anim_view.py       # GIF动画播放区（B区）
├── templates/
│   └── default.json
├── 开发文档/                   # 模块化开发文档
├── releases/
└── tests/
```

---

## 开发流程规范

### 需求分级与 PR 判断

每次接收到新需求时，按以下标准判断是否需要开 PR：

| 需求类型 | 是否开PR | 示例 |
|---------|---------|------|
| **新功能开发** | ✅ 必须 | 添加新部位、新增播放控制 |
| **架构重构** | ✅ 必须 | 模块拆分、API重构 |
| **UI/交互优化** | ✅ 必须 | 交互逻辑改变、新增设置项 |
| **Bug修复** | ❌ 可直接提交 | 修复崩溃、显示异常、逻辑错误 |
| **文档更新** | ❌ 可直接提交 | README、开发文档维护 |
| **代码微调** | ❌ 可直接提交 | 变量命名、注释补充、格式整理 |

### 开发流程

1. **需求分析** → 判断是否需要开 PR
2. **若需开 PR**：
   - 创建功能分支（命名如 `feature/add-fills-part`，**避免带斜杠可用纯名字**）
   - 创建需求方案文档（存放于 `开发文档/` 目录）
   - 开发、测试、调试
   - 完成后创建 PR，等待用户确认合并
3. **跟进合并** — PR 创建后主动询问用户是否合并入 main
4. **若不需开 PR** → 直接在 main 分支完成并提交

### 合并确认机制

- PR 创建完成后，必须询问用户：「PR 已创建，是否合并入 main 分支？」
- 用户确认后才执行合并操作
- 合并后及时更新相关开发文档

### Git 操作注意事项（本机环境）

- 所有 `git commit/push` 需在非沙箱模式执行（沙箱 overlay 会回滚 `.git/refs`）
- 尽早 push 到远端，本地 `.git` 不作为唯一锚点
- push 偶发 `schannel` 握手失败时，重试或改用 `git -c http.sslBackend=openssl push`

---

## 模块化开发文档管理

每次开发新功能或新需求时，按以下流程处理：

1. **分析模块归属** — 判断该功能属于哪个现有模块
2. **文档操作**：
   - 有对应模块 → 更新该模块的开发文档
   - 无对应模块 → 新建开发文档（命名清晰，如 `播放控制模块.md`）
3. **持续维护** — 后续围绕该模块的任何优化、调整、Bug修复，都要同步更新对应的开发文档

**目的**：确保开发文档与代码实现保持同步，方便追溯和查阅。

---

## UI设计规范

### 深色主题配色（Modern Dark Flat UI）

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | `#1E2023` | 深灰底色 |
| 卡片 | `#2A2E33` | 稍亮灰色 |
| 边框 | `#3A3F46` | 中灰边框 |
| 主色调 | `#D4AF37` | 金色强调（选中态） |
| 文字主色 | `#E8E4D9` | 米白文字 |
| 文字次色 | `#96A1AD` | 灰色文字 |
| 错误标记 | `#C74444` | 红色警告（缺失/缺帧） |

### 可读性要求

- 用户视力不佳：**正文字号 ≥ 14px**，按钮文字 ≥ 14px，关键状态可再放大
- 方向/动作使用**按钮全展开**，禁用下拉列表（减少点击层级）

### 缩略图显示规则

- **100%原尺寸显示**，不缩放
- **按包围盒裁剪**（非破坏性），只显示有效像素区域：
  - 动画播放用**全序列并集 bbox**（防抖动）
  - 网格单帧可用自身 bbox
- **无边框、透明背景**，融入UI

---

## 发布打包流程

### 打包命令

```powershell
# Windows 打包（在 venv 中执行）
pyinstaller --noconfirm --onefile --windowed --name MMY-FrameQuickView src/main.py
```

### 发布目录

```
releases/v{版本号}_{时间戳}/
├── MMY-FrameQuickView.exe
├── templates/          # 随包发布的规则模板
└── README.txt
```

---

### 注意事项

- 图像解码与 bbox 计算放工作线程，主线程只做显示，保证拖拽后秒开
- 大序列（数百帧）先做文件名索引再惰性解码，避免一次性读爆内存
